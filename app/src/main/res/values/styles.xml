<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="TextStyle" parent="AppTheme">
        <item name="android:gravity">center_horizontal</item>
        <item name="android:textSize">22sp</item>
        <item name="android:textColor">@color/colorAccent</item>
    </style>

    <style name="FragStyle" parent="TextStyle">
        <item name="android:textColor">@color/colorAccent</item>
        <item name="android:layout_marginStart">@dimen/border</item>
        <item name="android:layout_marginEnd">@dimen/border</item>
        <item name="android:backgroundTint">#BA484E74</item>
    </style>

    <style name="ButtonStyle" parent="Widget.MaterialComponents.Button">
        <item name="android:textSize">22sp</item>
        <item name="android:textColor">@color/colorAccent</item>
        <item name="android:backgroundTint">@color/colorPrimary</item>
        <item name="android:layout_marginStart">@dimen/border</item>
        <item name="android:layout_marginEnd">@dimen/border</item>
    </style>

    <style name="Title" parent="TextStyle">
        <item name="android:textSize">28sp</item>
    </style>

    <style name="MyToolbar" parent="ThemeOverlay.MaterialComponents.Toolbar.Primary">
    </style>
</resources>
